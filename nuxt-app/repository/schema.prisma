generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Monster {
  id Int @id @default(autoincrement())
  name String

  @@map("monsters")
}
